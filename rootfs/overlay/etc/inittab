# CinuxOS Buildroot base inittab
#
# 直接 respawn /bin/sh，不依赖 getty/ttyS0：
#   - CinuxOS DevFS 无 /dev/ttyS0 设备节点（只有 null/zero/console）
#   - busybox init 给 respawn 进程的 stdin/stdout 连 /dev/console（DevFS 有）
#   - 复用 F-ECO B3b 已验证的 ::respawn:/bin/sh 模式（~ # 提示符）
#
# 阶段2 起在此追加 ::once:/etc/cinux-usability-test.sh 跑可用性测试。

::sysinit:/bin/echo CinuxOS Buildroot init: userspace up
::respawn:/bin/sh
