# F9 批7:KRandom 内核 PRNG(M2 前置)

> 2026-06-25 · F9 M2(ASLR)前置 · `feat/f9-security`

## 改动

- **kernel/lib/random.hpp + random.cpp**:KRandom(init/next32/next64/fill)+ g_random。xoshiro256**(Blackman/Vigna,fast/high-quality/2^256 周期)。
- 熵源 init:rdrand(CPUID.01H:ECX[30] 检测,有则用)+ TSC(rdtsc)+ PIT::get_ticks() + 内核镜像地址(&g_random)。splitmix64 扩 seed → 256-bit state(避全零 xoshiro 卡死)。
- **kernel/lib/CMakeLists.txt**:加 random.cpp。
- **main.cpp**:PIT::init 后 g_random.init()。

## 诚实:test gap

KRandom **暂无独立 test**(违背 [[mechanism-test-and-debug-not-qemu]] 教训,记录在案):host 单测要 mock pit 依赖(PIT::get_ticks host 无);run-kernel-test test_random 多文件注册。**留批8 ASLR 间接验证**(ASLR 用 g_random.next 随机化,每次运行地址不同 = next 非零/变化)。PRNG 序列数学正确性(xoshiro)留 host 单测 follow-up。

## 关键点

- **诚实范围**:hobby kernel "够用"熵,非认证 CSPRNG。rdrand 最强;TSC/PIT/地址 mix 是 fallback(防 naive 猜)。
- **懒 init**:next32/next64 调时若没 init 自动 init(防漏调)。
- **splitmix64 seed expand** 避 all-zero xoshiro state(卡死)。

## 验证

- run-kernel-test **932/0**(KRandom 编译过不破坏;g_random production init,test 环境懒 init)。

## 下一步

批8 ASLR(ELF 加载/栈/mmap 地址随机化,用 g_random,间接验证 PRNG)。
