# F4-M1 批4:ACPIInfo 全局 + main 接线 + 真机探针 + M1 收尾

> 2026-06-19。F4-M1 批 4(收尾)。范围:g_acpi_info 全局 + init() + main.cpp 接线 + 启动探针 + PLAN/ROADMAP 更新。
> 基线 = M1-3(859/0)。**F4-M1(ACPI 静态表解析)4 批收官**。

## 目标
把 M1-3 的 parse_madt 接入 production 启动,存全局 g_acpi_info 供 M2,启动日志打探针(CPU/LAPIC/IOAPIC/override),收尾 M1 文档。

## 实现
- `acpi.hpp`:`extern ACPIInfo g_acpi_info` + `init()` 声明。
- `madt.cpp`:`g_acpi_info` 定义 + `init()`(find_rsdp → find_table("APIC") → parse_madt → g_acpi_info + kprintf 探针,static_cast 零警告;RSDP/MADT 缺失降级 defaults 不崩)。
- `main.cpp`:include acpi.hpp + PIT::init 后调 acpi::init()(direct-map 已建,早期可跑)。
- FADT/HPET 深度解析留 F5-M4(M1 只需 MADT = APIC 信息)。

## 验证
- big_kernel(production)编译 + 链接(main.cpp + acpi::init)成功。run-kernel-test **859/0**。零编译警告。
- production `make run` 冒烟:`[ACPI] 1 CPU(s), LAPIC 0xFEE00000, IOAPIC 0xFEC00000, 5 IRQ override(s), pcat=1`,启动到 GUI 不崩。

## M1 收尾总结
F4-M1(ACPI 静态表解析)4 批完成(840→859/0):RSDP 定位(M1-1,faabce1)→ find_table RSDT 主路径(M1-2,d862b01)→ MADT→ACPIInfo(M1-3,5216839)→ main 接线 + 探针(M1-4)。
关键 GOTCHA:①QEMU pc ACPI 1.0(仅 RSDT,无 XSDT)②log ANSI escape 用 grep -a ③ACPI 表 direct-map(cache-enabled OK)vs APIC MMIO(M2 需 VMM.map + FLAG_PCD)。真机 **5 IRQ override**(含 IRQ0→GSI2,M2-3 消费)。

## 下一步
F4-M2:LAPIC + IOAPIC + PIC→APIC 切换(xAPIC MMIO,qemu64 无 x2APIC)。
