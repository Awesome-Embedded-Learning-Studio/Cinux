# D5 + D6 deterministic 审计报告

> F-QA Q3-2。2026-06-21。分支 `feat/f-qa-q2`。Q2 deterministic 四段式。只读。

## 范围

D5 调度/迁移（例外：先读码后锚点）+ D6 用户/内核边界。D5 域 F4 SMP 已大量修（GOTCHA#23/25/26），审计确认 pass + 找残留；D6 查用户指针/ELF/VMA。

## A 锚点命中

### D5（先读码后锚点）

| # | 锚点 | 命中 |
|---|------|------|
| a1 per-CPU/current | current()/set_current/percpu()/cpu_id 命中（scheduler.cpp为核心） |
| a2 ctx switch/FPU/GS | context_switch/fxrstor/fs_base/gs_base/swapgs/tss_set_rsp0 |
| a3 runqueue/抢占 | runqueue/pick_next/add_task/dequeue/idle/yield/tick/quantum |

### D6

| # | 锚点 | 命中 |
|---|------|------|
| a1 用户指针 | validate_user_ptr/is_user_addr（path_util.hpp/sys_pipe.cpp）；**零 copy_from_user/copy_to_user** |
| a2 VMA/ELF 边界 | e_phnum/p_memsz/p_filesz（execve.cpp:188）；VmaFlags（exception_handlers.cpp:388/400） |

## B 不变点判定

### D5（F4 已修，确认 pass）

- [pass] **I1 current() per-CPU**：scheduler.cpp:231/243/251 `percpu()->current = next`。F4-M4 M4-1。
- [pass] **I2 context_switch 恢复点读 current()**：scheduler.cpp:239/257 `fxrstor current()->fpu_state`（读 per-CPU current，非局部 next）。GOTCHA#23 已修（M4-1）。
- [pass] **I3 GS/TLS/FPU 迁移**：`GDT::load()`（gdt.cpp）**不重载 %fs/%gs**（注释明确 + 只 `movw %ds/%es/%ss`）；syscall.S:55/131 swapgs 纪律。GOTCHA#25/26 已修（F4-M4 M4-2-3）。
- [pass] **I4 runqueue 多核安全 + 时间片**：per-CPU runqueue（F4-M4-2-2 pick_next）；quantum 内聚 RoundRobin（M4 批1）。

**D5 结论：F4 SMP 域清洁，核心不变量全 pass。无新发现。**

### D6

- [warn] **I1 用户指针 copy 风格**：`validate_user_ptr`（path_util.hpp:26）只查 canonical address，**非 copy_to_from_user**；直接解引用 + PF 兜底（F2-M5）。见 DEBT-019。
- [pass] **I2 写用户栈/信号帧验 VMA**：PF handler（exception_handlers.cpp:388/400）查 VmaFlags（Write/Anonymous）。F2-M5 硬门控。
- [pass] **I3 syscall 不泄漏内核指针**：ErrorOr→errno 翻译（A 边界）。
- [fail] **I4 ELF/长度/溢出**：execve.cpp:188 `phnum = ehdr->e_phnum`，validate_elf_header 只查 ==0，无上限。DEBT-012（3.6MB 分配）。

## 发现

### DEBT-019（新）— D6 I1 warn：用户指针 validate 后直接解引用（非 copy_to_from_user，PF 兜底）

Cinux 用户边界用 `validate_user_ptr`（canonical address 检查）+ 直接解引用，PF handler（F2-M5 硬门控：user PF 无 VMA→segfault）兜底。**非 Linux `copy_to_from_user` + `access_ok` 模型**。

根因：(1) validate 不查映射存在/权限/长度（多字节结构跨页未映射→PF；kernel-mode 解引用用户地址 err&0x04=0 容错零页）；(2) 多核 TOCTOU（user 另核改映射 + kernel 解引用 race）；(3) 不对齐 Linux copy 模型。当前单核 + 用户态串行不触发；多核用户态 + SMP 理论风险。P3（当前 PF 兜底可接受）。修：未来多核用户态时引入 copy_to_from_user（access_ok + 长度 + 页 copy）。

证据：path_util.hpp:26-39（validate 只查规范地址）/ grep `copy_from_user|copy_to_user` 零命中 / sys_stat.cpp:32 validate 后直接用。

### 关联确认（已知，非新）

- DEBT-012（D6 I4）：e_phnum 无上限 → 3.6MB 分配。✅。
- GOTCHA#11（D6）：PF 硬门控 user-mode 判定（err&0x04）。✅。

## C 证据门槛

- D5 3 锚点 + D6 2 锚点全跑命中进表。✅
- D5 I1-I4 全 pass（F4 证据：scheduler.cpp/gdt.cpp）；D6 I1 warn + I2/I3 pass + I4 fail。逐条 file:line。✅
- D5 无新发现（pass 清洁，非债确认 = F4 commit + GOTCHA#23/25/26 记录）。✅
- 去重：DEBT-012（D6 已记）/GOTCHA#11（D6 已记），DEBT-019 新（D6 特有）。

## D 闭环

- DEBT-019 新登记（D6，P3）。D5 无新（F4 已修，维度清洁）。
- 本报告：`reports/2026-06-21-d5-d6-audit.md`。维度计划 6/14（D2/D3/D4/D5/D6/D13）。

## 下个

Q3-3 候选：D7 错误韧性 + D11 模块组织，或 D9 静态工具 + D14 整数溢出。按用户「每批 2 个」节奏。
