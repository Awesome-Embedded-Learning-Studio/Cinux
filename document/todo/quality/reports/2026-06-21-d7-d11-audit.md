# D7 + D11 deterministic 审计报告

> F-QA Q3-3。2026-06-21。分支 `feat/f-qa-q2`。Q2 四段式。只读。

## 范围

D7 错误处理/崩溃韧性 + D11 模块组织/可维护性。D7 域 FO 已做（统一 panic + backtrace + memstats）；D11 查文件行数/重复/分层。

## A 锚点命中

### D7

| # | 锚点 | 命中 |
|---|------|------|
| a1 panic/OOM | kpanic/fatal_halt/OutOfMemory/return nullptr（scheduler/lockdep/exception_handlers/crt_stub/ubsan） |
| a2 诊断路径 | dump_registers/backtrace/dump_memory_stats/fatal_halt（exception_handlers.cpp FO 统一 panic） |

### D11

| # | 锚点 | 命中 |
|---|------|------|
| a1 文件行数 | 源 max process.hpp=496（全 <500）；test max test_fork_exec=1182 |
| a2 重复/helper | 类内 "Internal helpers"（terminal/window/execve，正常） |

## B 不变点判定

### D7（FO 已做，确认 pass）

- [pass] **I1 panic 仅不变量**：kpanic 用于栈溢出（exception_handlers.cpp:300/331 guard page）、lockdep AB-BA（lockdep.cpp:123/135/149）、assert（crt_stub.cpp:81）、UB（ubsan.cpp:36）。可恢复错误走 ErrorOr/errno。
- [pass] **I2 OOM 可诊断**：ErrorOr OutOfMemory（ram_block_device/slab）+ slab nullptr。非静默。
- [pass] **I3 panic 路径不依赖危险锁/堆**：FO 防御 backtrace（栈范围）+ dump_memory_stats；PF handler lock-free（exception_handlers.cpp:340 注释）。
- [pass] **I4 日志格式**：FO GOTCHA（kprintf %zu）已避。

**D7 结论：FO 域清洁，全 pass。无新发现。**

### D11

- [pass] **I1 源 ≤500**：源文件（非 test）全 <500（max process.hpp 496）。CI check_line_limits --cpp 500 过。
- [pass] **I2 职责聚焦**：大源文件（ext2/terminal/scheduler/exception_handlers 400-450）职责单一。
- [pass] **I3 无重复**：helpers 是类内（正常），非跨文件重复实现。
- [warn] **I4 test 文件大**：test_fork_exec 1182 / test_cwd_stat 890 / test_scheduler 760 等。**check_line_limits 排除 test/**（L42 `if "test" in parts: continue`），故不违规，但大 test 文件是可维护性异味。

## 发现

### CI check_line_limits 配置（核实，补充 memory）

`scripts/check_line_limits.py` 排除 test/ 与 mini/（L40-43 `if "test"/"mini" in parts: continue`），只门禁 kernel/ 源文件。CI（ci.yml line-limits job）跑 `--hpp 500`（默认 300 放宽）。源文件全 <500（max process.hpp 496）；test/ 豁免（test_fork_exec 1182 不违规）。memory `file-500-line-limit-split` 称「源 500 软上限」准确，本审计补充 CI 配置细节（test/ 豁免）。

### test 文件大（观察，P3，非债）

test_fork_exec 1182 等大 test 文件。check_line_limits 豁免 test/（合理：test 集中 fixture）。可选优化：超大 test（>800）按场景拆。非急。

## C 证据门槛

- D7 2 锚点 + D11 2 锚点全跑命中进表。✅
- D7 I1-I4 全 pass（FO 证据）；D11 I1-I3 pass + I4 warn。逐条 file:line。✅
- D7/D11 无新 P0/P1（FO + 源码清洁）。非债确认（D7 panic 用法）有反例：可恢复错误走 ErrorOr（A 铁律）。

## D 闭环

- 无新 DEBT（D7 FO 清洁 / D11 源 <500）。memory 订正（check_line_limits 排除 test/）。
- 本报告：`reports/2026-06-21-d7-d11-audit.md`。维度计划 8/14（D2/D3/D4/D5/D6/D7/D11/D13）。

## 下个

Q3-4 候选：D9 静态工具 + D14 整数溢出（收尾交接重点 D14），或 D1/D8/D10/D12。
